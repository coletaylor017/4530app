<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs4530.lifestyleapp">
    <!-- Camera added to manifest, permissions true for all devices added -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <!-- Location request https://developer.android.com/training/permissions/requesting#already-granted -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileDisplayActivity"
            android:label="ProfileDisplayActivity"
            android:parentActivityName=".MainActivity"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"
                />
        </activity>
        <!-- Not 100% on the name here https://developer.android.com/training/location/permissions-->
        <service
            android:foregroundServiceType="location"
            android:name="com.cs4530.lifestyleapp.ProfileDisplayActivity"
            />
        <!-- As a note this service seemed to be the key to getting the code working -->

    </application>

</manifest>